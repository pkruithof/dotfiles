# overwritten by runcoms/zhsrc

source ~/.zsh/config
source ~/.zsh/input

#source ~/.common/aliases
#source ~/.common/functions
source ~/.common/exports

# use .localrc for settings specific to one system
[[ -f ~/.localrc ]] && . ~/.localrc